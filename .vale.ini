StylesPath = .github/styles
MinAlertLevel = warning

[*.md]
BasedOnStyles = SB

# This bootstrap file intentionally names foreign projects only as explicit
# anti-import examples, so the contamination rule must not treat those quoted
# prohibitions as actual project leakage.
[docs/NEW_CHAT_BOOTSTRAP_PROMPT.md]
SB.ForeignProject = NO

[*.{yaml,yml}]
BasedOnStyles = SB
